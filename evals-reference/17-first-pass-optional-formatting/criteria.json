{
  "context": "The skill should write new Optional code using a direct Optional boundary, not presence-check/value-read, null, or fake collection control flow.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Creates requested class",
      "max_score": 4,
      "description": "Creates AssigneeFormatter.java with AssigneeFormatter, label(Optional<User>), and nested User record."
    },
    {
      "name": "Uses direct Optional boundary",
      "max_score": 8,
      "description": "Uses assignee.map(...).orElse(\"unassigned\") or an equivalent direct Optional boundary for present/absent handling."
    },
    {
      "name": "No presence-check value read",
      "max_score": 8,
      "description": "Does not use isPresent()/isEmpty() plus get() or orElseThrow() for ordinary control flow."
    },
    {
      "name": "No null or fake collection workaround",
      "max_score": 6,
      "description": "Does not use orElse(null), local null branching, Optional.stream().toList(), or a loop over one Optional."
    },
    {
      "name": "Preserves formatting behavior",
      "max_score": 8,
      "description": "Returns @handle for present nonblank handles, displayName for present blank handles, and unassigned when absent."
    }
  ],
  "metadata": {
    "invocation": "explicit",
    "task_type": "implementation"
  }
}
