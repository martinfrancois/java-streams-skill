{
  "context": "The skill should reject cleanups that preserve Optional reopening and repeated get() reads.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Rejects proposed cleanup",
      "max_score": 10,
      "description": "Says the proposed change should not be accepted as an Optional cleanup."
    },
    {
      "name": "Identifies repeated get",
      "max_score": 10,
      "description": "Explains that the proposed code still uses target.get() for ordinary value reads after a presence check, including repeated reopening."
    },
    {
      "name": "Suggests binding once",
      "max_score": 12,
      "description": "Suggests target.map(list -> ...).orElse(card), a named helper, or binding ListRef once after a narrow guard."
    },
    {
      "name": "No null or fake collection workaround",
      "max_score": 8,
      "description": "Does not suggest replacing repeated get() with orElse(null), null checks, Optional.stream().toList(), or a loop over one Optional."
    }
  ],
  "metadata": {
    "invocation": "explicit",
    "task_type": "review"
  }
}
