{
  "context": "The skill should reject bundling DTO/API redesign into a local Optional cleanup, especially by putting Optional into a legacy request record.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Rejects bundled API change",
      "max_score": 12,
      "description": "Says the proposed change should not be accepted as a local Optional cleanup because it changes the LegacyRequest API/DTO boundary."
    },
    {
      "name": "Recognizes nullable interop",
      "max_score": 8,
      "description": "Recognizes that the original orElse(null) can be an isolated legacy interop boundary."
    },
    {
      "name": "Avoids Optional DTO field",
      "max_score": 10,
      "description": "Does not endorse changing the DTO/record field to Optional as part of this cleanup."
    },
    {
      "name": "Separates design work",
      "max_score": 10,
      "description": "Frames any DTO redesign as separate scoped API/design work requiring compatibility review."
    }
  ],
  "metadata": {
    "invocation": "explicit",
    "task_type": "review"
  }
}
