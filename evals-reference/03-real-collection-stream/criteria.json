{
  "context": "The skill should distinguish a real collection stream from a single Optional being forced through stream syntax.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Recognizes real collection source",
      "max_score": 8,
      "description": "Identifies SECRET_OPTIONS as a real collection/set lookup, not a single Optional workaround."
    },
    {
      "name": "Keeps readable stream",
      "max_score": 10,
      "description": "Keeps the stream lookup or says no refactor is needed. Does not rewrite to a manual loop merely to avoid an Optional terminal result."
    },
    {
      "name": "findAny rationale",
      "max_score": 6,
      "description": "Mentions findAny is appropriate because all matching options are equivalent."
    }
  ],
  "metadata": {
    "invocation": "explicit",
    "task_type": "review"
  }
}
