{
  "context": "The skill should write a real Set lookup as a stream returning Optional and choose findAny when any match is equivalent.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Creates requested matcher",
      "max_score": 4,
      "description": "Creates CommandOptionMatcher.java with the requested REDACTED_VALUE_OPTIONS set and static redactedValueOption(String arg)."
    },
    {
      "name": "Uses real collection stream",
      "max_score": 10,
      "description": "Uses REDACTED_VALUE_OPTIONS.stream().filter(...) or an equivalent readable real collection lookup rather than treating the lookup as a single Optional workaround."
    },
    {
      "name": "Uses findAny",
      "max_score": 10,
      "description": "Uses findAny() because any matching redacted option is equivalent."
    },
    {
      "name": "Preserves matching behavior",
      "max_score": 10,
      "description": "Matches both exact option arguments and option=value forms, and returns Optional.empty() when no option matches."
    },
    {
      "name": "No null workaround",
      "max_score": 6,
      "description": "Does not use null sentinels, orElse(null), or local null-control-flow."
    }
  ],
  "metadata": {
    "invocation": "explicit",
    "task_type": "implementation"
  }
}
