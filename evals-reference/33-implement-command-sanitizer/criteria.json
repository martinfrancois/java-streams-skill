{
  "context": "This mirrors the observed real-collection stream overcorrection: matching should remain a readable Set lookup while the outer loop handles sequence state.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Creates requested sanitizer",
      "max_score": 6,
      "description": "Creates CommandSanitizer.java with sanitize(List<String>) and the required secret option set."
    },
    {
      "name": "Uses real collection Optional lookup",
      "max_score": 18,
      "description": "Centralizes option matching in a readable REDACTED_VALUE_OPTIONS.stream().filter(...).findAny() helper or equivalent real collection stream returning Optional."
    },
    {
      "name": "Uses findAny for equivalent matches",
      "max_score": 10,
      "description": "Uses findAny() for the option set because any matching secret option is equivalent."
    },
    {
      "name": "Uses Optional result directly",
      "max_score": 14,
      "description": "Handles the Optional match with map/orElse or another direct Optional boundary rather than isPresent()/orElseThrow() value reads."
    },
    {
      "name": "Avoids overcomplicated loop control",
      "max_score": 10,
      "description": "Does not replace the collection lookup with nested loops, labels, duplicated matching branches, or sentinel logic beyond the necessary redact-next state."
    },
    {
      "name": "Preserves sanitizer behavior",
      "max_score": 22,
      "description": "Correctly handles exact options, option=value forms, redacts the following value only after exact options, preserves non-secret arguments, and joins with spaces."
    }
  ],
  "metadata": {
    "invocation": "explicit",
    "task_type": "implementation"
  }
}
