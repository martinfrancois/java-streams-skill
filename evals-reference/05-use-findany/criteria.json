{
  "context": "The skill should use findAny for an order-independent Set lookup where all matches are equivalent.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Uses findAny",
      "max_score": 10,
      "description": "Changes findFirst to findAny or clearly states findAny is the intended shape."
    },
    {
      "name": "Preserves real collection stream",
      "max_score": 6,
      "description": "Recognizes the stream is over a real collection and does not replace it with a manual loop or null-control-flow."
    }
  ],
  "metadata": {
    "invocation": "explicit",
    "task_type": "cleanup"
  }
}
