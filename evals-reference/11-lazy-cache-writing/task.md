# Write first-pass lazy Optional cache code

Assume Java 17.

Use `$java-optionals` to write first-pass Java code for this requirement:

Return an existing cached document if present. If it is absent, create a document, store it in the
cache, and return it.
