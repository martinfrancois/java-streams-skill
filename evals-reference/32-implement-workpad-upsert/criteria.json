{
  "context": "This mirrors the observed production implementation failure: an update-or-create flow should use Optional as the decision boundary and keep creation lazy.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Creates requested service",
      "max_score": 6,
      "description": "Creates WorkpadService.java with upsertWorkpad(Card, String), the requested records, and needed imports."
    },
    {
      "name": "Uses Optional update/create boundary",
      "max_score": 18,
      "description": "Uses the result of the workpad lookup as the update-or-create boundary, such as findFirst().map(...).orElseGet(...), with named helpers if useful."
    },
    {
      "name": "Preserves lazy create path",
      "max_score": 14,
      "description": "Does not execute comment-window checks or create logic when an existing workpad comment is present; uses orElseGet or an equivalent lazy branch."
    },
    {
      "name": "No nullable or fake collection workaround",
      "max_score": 12,
      "description": "Does not use orElse(null) plus local null branching, Optional.stream().toList(), a list, or a loop over one Optional to unwrap the workpad."
    },
    {
      "name": "No presence-check value read",
      "max_score": 12,
      "description": "Does not use isPresent()/isEmpty() followed by get() or orElseThrow() for ordinary workpad value reads."
    },
    {
      "name": "Preserves all statuses",
      "max_score": 18,
      "description": "Returns missing_action_id, comment_window_incomplete, updated:id:text, and created:cardId:text in the required cases."
    }
  ],
  "metadata": {
    "invocation": "explicit",
    "task_type": "implementation"
  }
}
