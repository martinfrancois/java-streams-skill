{
  "context": "Main eval weighted natural implementation: second-lowest without-context baseline in the all-eval run; Java 24 preview-enabled blocking per-element remote checks with a per-operation service concurrency limit should use a bounded stream concurrency shape rather than parallelStream or unbounded async work.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Creates coherent Java 24 preview artifact",
      "category": "safety",
      "max_score": 20,
      "description": "Creates FavoriteProducts.java with the requested method, nested types, imports, and Java 24 preview-compatible code."
    },
    {
      "name": "Preserves favorite product behavior",
      "category": "safety",
      "max_score": 40,
      "description": "Returns only products whose sku passes InventoryApi.check and sorts the final returned products by Product::name."
    },
    {
      "name": "Uses bounded Gatherers.mapConcurrent",
      "category": "stream_quality",
      "max_score": 180,
      "description": "Uses Gatherers.mapConcurrent with an explicit max concurrency no greater than 8 to perform the blocking InventoryApi.check calls inside the stream chain."
    },
    {
      "name": "Carries product with stock result",
      "category": "stream_quality",
      "max_score": 80,
      "description": "Keeps each Product associated with its stock-check result, for example with Map.entry(product, InventoryApi.check(product.sku())), then filters by the boolean result before mapping back to Product."
    },
    {
      "name": "Rejects weak concurrency choices",
      "category": "stream_quality",
      "max_score": 60,
      "description": "Does not use parallelStream(), .parallel(), unbounded CompletableFuture fan-out, or shared mutable state for collecting remote check results."
    },
    {
      "name": "Keeps implementation focused",
      "category": "maintainability",
      "max_score": 20,
      "description": "Avoids unrelated caching, retries, dependencies, API redesign, or broad concurrency framework changes."
    }
  ],
  "metadata": {
    "invocation": "natural",
    "task_type": "implementation",
    "main_eval_weight_multiplier": 4,
    "main_eval_selection": "selected from all-26 Sonnet 4.6 run because at least one without-context criterion scored below 50%"
  }
}
